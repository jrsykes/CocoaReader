sweep_config:
  method: random
metric:
  name: Best_F1
  goal: maximize
parameters:
    input_size:
        distribution: uniform
        min: 200
        max: 400
    loss_fn:
        values: ['CrossEntropyLoss', 'DynamicFocalLoss']
    
      
      